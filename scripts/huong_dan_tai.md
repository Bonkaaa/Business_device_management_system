1. Windows
`.\scripts\build.bat`

2. Powershell
`.\scripts\build.ps1`

3. Linux/Mac/Git bash
`./scripts/install_app.sh`

4. Using main.py
`python -m venv .venv`
`pip install -r requirements.txt`
`python -m src.main`